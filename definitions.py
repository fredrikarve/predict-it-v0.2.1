import os

# The root_dir variable defines the root directory of the project.
# The reason for it to be in this definitions file is that it needs to
# be in a file placed in the root directory.
root_dir = os.path.dirname(os.path.abspath(__file__))


